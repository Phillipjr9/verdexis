version: 1
applications:
  - appRoot: app
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Setting NODE_ENV to production for build..."
            - export NODE_ENV=production
            - echo "Setting API URL to Elastic Beanstalk backend..."
            - export VITE_API_URL=https://verdexis-api-env.eba-dcptnmmg.us-east-1.elasticbeanstalk.com/api
            - echo "Installing all dependencies including devDependencies..."
            - npm ci --include=dev
            - echo "Dependencies installed successfully"
            - echo "Verifying vite plugins installation..."
            - ls -la node_modules/@vitejs || echo "@vitejs not found"
        build:
          commands:
            - echo "Adding node_modules/.bin to PATH..."
            - export PATH="$PATH:$PWD/node_modules/.bin"
            - echo "Starting build with vite..."
            - npm run build
            - echo "Build completed successfully"
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
