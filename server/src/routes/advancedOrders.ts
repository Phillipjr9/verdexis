import { Router } from 'express'

const router = Router()

// Advanced orders routes placeholder - not implemented yet
router.get('/', (req, res) => {
  res.json({ orders: [] })
})

export default router
