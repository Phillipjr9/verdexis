{
  "scripts": {
    "build": "node build.js",
    "build:fast": "npm --prefix server run build && npm --prefix app run build",
    "vercel-build": "node build.js",
    "deploy:vercel": "npm run build && npx vercel --prod --confirm"
  },
  "dependencies": {
    "@auth0/nextjs-auth0": "^4.26.0",
    "@vercel/node": "^5.5.12",
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.1.8",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@types/node": "^22.20.1",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.9.1",
    "typescript": "^5.7.0",
    "viem": "^2.55.0"
  },
  "overrides": {
    "ajv": "^8.18.0",
    "path-to-regexp": "^6.3.0",
    "undici": "^7.29.0",
    "uuid": "^14.0.0",
    "cookie": "^0.7.0"
  }
}
